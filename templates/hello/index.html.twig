{% extends 'base.html.twig' %}


{% block body %}
<div> 
    {% if  messages|length > 0 %}
    {% for message in messages %}
        <div>{{ message }}</div>
    {% endfor %}
    {% else %}
        There's nothing here yet!
    {% endif %}
</div>
{% endblock %}